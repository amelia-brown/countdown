import Digits from '../src/components/Digits';

describe('this is a test', () => {
  it('runs a test', () => {
    expect(1).toEqual(1);
  });
  it('changes every second', () => {
    expect()
  });
})
